# Vue 模板语法与指令系统

## 文本插值与绑定
- 双大括号文本插值
- v-text 与 v-html 指令
- 动态属性绑定 v-bind
- 表达式语法与限制
- 过滤器使用(Vue2)与计算属性替代(Vue3)

## 条件渲染
- v-if/v-else-if/v-else 条件渲染
- v-show 与 v-if 性能对比
- key 属性与条件渲染
- 模板复用与缓存
- 条件渲染最佳实践

## 列表渲染
- v-for 指令与数组渲染
- 对象与范围值遍历
- key 属性与列表更新
- 数组变更检测原理
- 高性能大列表渲染

## 事件处理
- v-on 事件绑定
- 事件处理方法与内联表达式
- 事件修饰符与组合使用
- 按键修饰符与系统修饰符
- 自定义事件处理与传参

## 表单输入绑定
- v-model 双向绑定原理
- 表单元素绑定技术
- 修饰符(.lazy/.number/.trim)
- 自定义组件上的 v-model
- 多值绑定与动态绑定

## 高级指令
- v-slot 插槽指令系统
- v-pre 跳过编译
- v-once 一次性渲染
- v-cloak 解决闪烁问题
- v-memo 渲染缓存(Vue3)

## 自定义指令
- 自定义指令注册方法
- 指令钩子函数与生命周期
- 指令参数与修饰符访问
- 复杂逻辑封装案例
- 常用自定义指令实现

## 模板编译原理
- 模板编译流程
- 渲染函数与虚拟DOM
- JSX 与模板对比
- 静态提升与缓存
- 编译优化技术

> 注：本文档会持续更新，欢迎关注！ 